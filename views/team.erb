<!DOCTYPE html>
<html>
 <head>
  <meta charset="UTF-8">
  <title>Basketball Team</title>
 </head>
 <body>
  <% @params.each do |key, value| %>
    <% if key == "name" %>
      <h1><%= key %>: <%= value %></h1>
    <% else %>
      <h2><%= key %>: <%= value %></h2>
    <% end %>
 </body>
</html>